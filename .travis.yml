language: go
install: go get -v github.com/spf13/hugo
script:
  - hugo
  - ls -l
  - git clone https://github.com/fuegowolf/cocoa-eh-hugo-theme.git
  - cd philippvk.github.io/
  - git checkout master
  - git pull
  - cp -r ../public/* .
  - ls -l
  - git add .
  - git commit -m "Upload to GitHub Pages"
  - git push
  - rm -rf public/
  - ls -l
notifications:
    email:
        on_failure: always
